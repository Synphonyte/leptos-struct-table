A table example with a data source that uses server functions and sqlx.

If you don't have `cargo-leptos` installed you can install it with

```bash
cargo install cargo-leptos
```

To run the example execute

```bash
cargo leptos watch
```
